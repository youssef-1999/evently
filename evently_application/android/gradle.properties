org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Djavax.net.ssl.trustStore="C:\\Users\\Youssef AbdElBaky\\.gradle\\cacerts-with-avg.jks" -Djavax.net.ssl.trustStorePassword=changeit
android.useAndroidX=true
# Kotlin's incremental compiler crashes on Windows when the project and the
# Gradle/Pub caches are on different drive letters (can't compute a relative
# path across drives). Disable incremental Kotlin compilation to avoid it.
kotlin.incremental=false
# Use Android Studio's bundled JBR instead of the system JDK: the system JDK
# (Oracle 26) fails TLS handshakes against dl.google.com (PKIX path errors).
org.gradle.java.home=C:\\Program Files\\Android\\Android Studio\\jbr
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
